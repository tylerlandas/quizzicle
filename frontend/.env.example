# Base URL of the backend API. Leave unset for local dev — Vite proxies
# '/api' to the local backend (see vite.config.ts). Set this on Vercel to
# your deployed backend's URL, e.g.:
# VITE_API_URL=https://quizzicle-api.onrender.com/api
VITE_API_URL=
